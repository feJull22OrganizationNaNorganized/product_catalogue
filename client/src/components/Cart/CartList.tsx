import React from 'react';
import { Iphones } from '../../types/Iphones';
import { Cart } from '../Cart/Cart';

// type Props = {
//   cart: Iphones[];
//   updateCart: (item: Iphones, newCount: number, remove?: boolean) => void;
// }

// export const CartList:React.FC<Props> = ({ cart, updateCart }) => {
//   return (
//     <ul >
//       {cart.map(cartItem => (
//         <Cart key={cartItem.id} cartItem={cartItem} updateCart={updateCart} />
//       ))}
//     </ul>
//   );
// };